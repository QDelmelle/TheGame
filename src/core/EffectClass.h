#pragma once

#include "Body.h"

enum effect {
	PHYSICAL,
	MAGIC,
	POISON,
	BURN,
	SLOW,
	STUN,
	ONESHOT,
	BLEED,
	HEAL,
	SPEEDUP,
	REVIVE,
	INVINCIBLE,
	INVISIBLE,
};

class EffectClass {
public:
	
};